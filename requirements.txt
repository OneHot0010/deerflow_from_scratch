# P0: model calls (chat + embedding) via the Volcengine Ark SDK.
volcengine-python-sdk>=5.0.48

# P2: expose the agent over HTTP with token-level SSE streaming.
fastapi>=0.110
uvicorn>=0.29

# Tests (P0-P2): offline unit + SSE integration suite.
pytest>=8

# P6: models.yaml config parsing (with a tiny fallback if PyYAML is absent).
PyYAML>=6

# P7: MCP tool integration adds *zero* new dependencies — the stdio / SSE / HTTP
# transports speak MCP JSON-RPC directly over the stdlib (``subprocess`` +
# ``urllib``). ``mcp.yaml`` is parsed with the same PyYAML (with tiny fallback)
# already listed above. The official ``mcp`` SDK / ``langchain-mcp-adapters``
# remain aspirational backends that can slot in behind ``MCPSession`` later.
